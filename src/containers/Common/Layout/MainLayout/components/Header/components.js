import styled from 'styled-components';
import { Layout } from 'antd';

const HeaderContainer = styled(Layout.Header)`
  display: flex;
  justify-content: space-between;
`;

const HeaderBloc = styled.div`
  display: flex;
`;

export { HeaderContainer, HeaderBloc };

